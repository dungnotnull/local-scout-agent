[project]
name = "local-scout-agent"
version = "0.1.0"
description = "AI-powered guide to authentic local dining — cut through tourist traps and fake KOL reviews"
requires-python = ">=3.11"
readme = "CLAUDE.md"
license = { text = "MIT" }

[project.scripts]
scout = "app.cli:main"

[project.optional-dependencies]
dev = ["pytest>=8.2.0", "pytest-asyncio>=0.23.7", "ruff>=0.4.0", "black>=24.4.0", "mypy>=1.10.0"]
all = []
